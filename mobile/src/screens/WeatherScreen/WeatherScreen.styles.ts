import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  weatherScreen: {
    flex: 1,
    gap: 16,
    flexDirection: "column",

    padding: 12,
  },
  safeArea: {
    padding: 10,
  },
});
