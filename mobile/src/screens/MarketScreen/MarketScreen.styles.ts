import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  marketScreen: {
    display: "flex",
    flexDirection: "column",
    gap: 16,

    padding: 12,
  },
  header: {
    borderRadius: 10,
    overflow: "hidden",
  },
  loader: {
    marginTop: 100,
  },
})
